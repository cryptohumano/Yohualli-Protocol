// SPDX-License-Identifier: Apache-2.0
pragma solidity ^0.8.28;

/// @dev Prueba mínima PVM (mismo que en `yohualli_honk_verifier`) para validar
///     resolc + despliegue vía `hardhat ignition` en Polkadot Hub.
contract HubCreateProbe {
    // Sin storage: solo comprobar pipeline.
}
